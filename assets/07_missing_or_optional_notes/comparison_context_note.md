# Comparison Context Note

The bundle’s primary run artifacts come from:

- `data/processed/final_quality_acceptance/post_calibration_run`

The included before/after calibration comparison files were copied from:

- `data/processed/final_quality_acceptance/reports/`

Those stored comparison artifacts currently reference:

- `before_run = data/processed/final_quality_acceptance/pre_calibration_run`
- `after_run = data/processed/final_quality_acceptance/sanity_post2`

This means the comparison files are useful for understanding the documented calibration pass and its stored comparison outputs, but they are not a fresh re-derived comparison against the bundled `post_calibration_run`.
