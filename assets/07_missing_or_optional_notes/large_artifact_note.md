# Large Artifact Note

The full candidate pool for the primary run was not copied into this bundle because it is too large to be reviewer-friendly:

- Omitted file: `data/processed/final_quality_acceptance/post_calibration_run/candidate_pool.json`
- Approximate size: `224 MB`
- Reason omitted: too large for a compact external-review bundle

Included instead:

- `02_topk_and_candidates/candidate_pool_sample.json`

That sample preserves original candidate entries and includes:

- the first `50` selected candidates in source order
- the first `50` non-selected candidates in source order

If a reviewer needs the full pool later, it remains available at the original repo path above.
