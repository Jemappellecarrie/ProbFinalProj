export function LoadingState() {
  return (
    <section className="panel status-panel">
      <p className="eyebrow">Working</p>
      <h2>Building a demo puzzle...</h2>
      <p>The pipeline is loading seed data, generating groups, and preparing trace metadata.</p>
    </section>
  );
}
